When NestJS registers a controller route handler, how does an incoming request get run through its guards, interceptors, and pipes before the controller method is invoked? Trace the call path from `RouterExplorer.createCallbackProxy` into `RouterExecutionContext.create` and down through `GuardsConsumer.tryActivate`, `InterceptorsConsumer.intercept`, and `PipesConsumer.apply` to the wrapped handler.
