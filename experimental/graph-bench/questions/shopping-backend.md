When a seller calls `POST /shoppings/sellers/sales` and `ShoppingSellerSaleController.create()` runs, how does the new sale end up written to the database together with its first snapshot? Trace the call path from the controller method through `ShoppingSaleProvider.create` and `ShoppingSaleSnapshotProvider.collect` down to the Prisma `shopping_sales.create` write that persists the sale and its nested snapshot.
