Compiler diagnostics: TypeScript type errors, plus the project's @ttsc/lint rule violations and transform-plugin (typia, nestia) findings when present, each with its code and location exactly as ttsc reports them. Pass a `file` for one file's errors, or omit it for every current finding across the project, grouped by file; use that after an edit to see what is now broken. Pass `severity` to keep only errors or only warnings.
